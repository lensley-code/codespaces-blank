## Cursor Cloud specific instructions

### Repository overview

This is a new/skeleton repository with a single `frontend/` directory containing an empty `README.md`. As of the initial commit there is:

- No application code, build system, or package manager configuration
- No tests, linting, or CI/CD pipeline
- No Docker or devcontainer setup
- No external service dependencies

### Development environment

No dependencies need to be installed. The `frontend/` directory suggests a frontend web application will be added in the future. When application code is introduced, update this section with:

- The package manager and install command (e.g. `pnpm install`)
- How to run the dev server, tests, and linter
- Any required environment variables or external services
