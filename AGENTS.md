# AGENTS.md

## Cursor Cloud specific instructions

This is a skeleton repository with a `frontend/` directory stub containing an empty `README.md`. No application code, dependencies, or services exist yet.

- **No services to run.** The repo has no backend, frontend build, database, or other services.
- **No dependencies to install.** There are no `package.json`, `requirements.txt`, or other dependency manifests.
- **No lint/test/build commands.** There are no configured linters, test suites, or build scripts.

When application code is added, update this file with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`)
- How to run the dev server(s)
- How to run lint, tests, and builds
- Any non-obvious caveats discovered during setup
